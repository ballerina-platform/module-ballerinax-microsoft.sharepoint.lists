// AUTO-GENERATED FILE. DO NOT MODIFY.
// This file is auto-generated by the Ballerina OpenAPI tool.

// Copyright (c) 2026, WSO2 LLC. (http://www.wso2.com).
//
// WSO2 LLC. licenses this file to you under the Apache License,
// Version 2.0 (the "License"); you may not use this file except
// in compliance with the License.
// You may obtain a copy of the License at
//
// http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing,
// software distributed under the License is distributed on an
// "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
// KIND, either express or implied.  See the License for the
// specific language governing permissions and limitations
// under the License.

import ballerina/data.jsondata;
import ballerina/http;

# SharePoint subset of the Microsoft Graph v1.0 OpenAPI specification (generated from graphexplorer.yaml).
public isolated client class Client {
    final http:Client clientEp;
    # Gets invoked to initialize the `connector`.
    #
    # + config - The configurations to be used when initializing the `connector` 
    # + serviceUrl - URL of the target service 
    # + return - An error if connector initialization failed 
    public isolated function init(ConnectionConfig config, string serviceUrl = "https://graph.microsoft.com/v1.0/sites") returns error? {
        http:ClientConfiguration httpClientConfig = {auth: config.auth, httpVersion: config.httpVersion, http1Settings: config.http1Settings, http2Settings: config.http2Settings, timeout: config.timeout, forwarded: config.forwarded, followRedirects: config.followRedirects, poolConfig: config.poolConfig, cache: config.cache, compression: config.compression, circuitBreaker: config.circuitBreaker, retryConfig: config.retryConfig, cookieConfig: config.cookieConfig, responseLimits: config.responseLimits, secureSocket: config.secureSocket, proxy: config.proxy, socketConfig: config.socketConfig, validation: config.validation, laxDataBinding: config.laxDataBinding};
        self.clientEp = check new (serviceUrl, httpClientConfig);
    }

    # Get lists in a site
    #
    # + siteId - The unique identifier of site
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved collection 
    remote isolated function listLists(string siteId, map<string|string[]> headers = {}, *ListListsQueries queries) returns MicrosoftGraphListCollectionResponse|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a new list
    #
    # + siteId - The unique identifier of site
    # + headers - Headers to be sent with the request 
    # + payload - New navigation property 
    # + return - Created navigation property 
    remote isolated function createList(string siteId, MicrosoftGraphList payload, map<string|string[]> headers = {}) returns MicrosoftGraphList|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get metadata for a list
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved navigation property 
    remote isolated function getList(string siteId, string listId, map<string|string[]> headers = {}, *GetListQueries queries) returns MicrosoftGraphList|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete navigation property lists for sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteList(string siteId, string listId, DeleteListHeaders headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update the navigation property lists in sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + payload - New navigation property values 
    # + return - Success 
    remote isolated function updateList(string siteId, string listId, MicrosoftGraphList payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # List columnDefinitions in a list
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved collection 
    remote isolated function listColumns(string siteId, string listId, map<string|string[]> headers = {}, *ListColumnsQueries queries) returns MicrosoftGraphColumnDefinitionCollectionResponse|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/columns`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a columnDefinition in a list
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + payload - New navigation property 
    # + return - Created navigation property 
    remote isolated function createColumn(string siteId, string listId, MicrosoftGraphColumnDefinition payload, map<string|string[]> headers = {}) returns MicrosoftGraphColumnDefinition|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/columns`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get columns from sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + columnDefinitionId - The unique identifier of columnDefinition
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved navigation property 
    remote isolated function getColumn(string siteId, string listId, string columnDefinitionId, map<string|string[]> headers = {}, *GetColumnQueries queries) returns MicrosoftGraphColumnDefinition|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/columns/${getEncodedUri(columnDefinitionId)}`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete navigation property columns for sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + columnDefinitionId - The unique identifier of columnDefinition
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteColumn(string siteId, string listId, string columnDefinitionId, DeleteColumnHeaders headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/columns/${getEncodedUri(columnDefinitionId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update the navigation property columns in sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + columnDefinitionId - The unique identifier of columnDefinition
    # + headers - Headers to be sent with the request 
    # + payload - New navigation property values 
    # + return - Success 
    remote isolated function updateColumn(string siteId, string listId, string columnDefinitionId, MicrosoftGraphColumnDefinition payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/columns/${getEncodedUri(columnDefinitionId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get sourceColumn from sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + columnDefinitionId - The unique identifier of columnDefinition
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved navigation property 
    remote isolated function getColumnSourceColumn(string siteId, string listId, string columnDefinitionId, map<string|string[]> headers = {}, *GetColumnSourceColumnQueries queries) returns MicrosoftGraphColumnDefinition|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/columns/${getEncodedUri(columnDefinitionId)}/sourceColumn`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get the number of the resource
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The count of the resource 
    remote isolated function getColumnsCount(string siteId, string listId, map<string|string[]> headers = {}, *GetColumnsCountQueries queries) returns string|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/columns/$count`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # List contentTypes in a list
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved collection 
    remote isolated function listContentTypes(string siteId, string listId, map<string|string[]> headers = {}, *ListContentTypesQueries queries) returns MicrosoftGraphContentTypeCollectionResponse|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create new navigation property to contentTypes for sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + payload - New navigation property 
    # + return - Created navigation property 
    remote isolated function createContentType(string siteId, string listId, MicrosoftGraphContentType payload, map<string|string[]> headers = {}) returns MicrosoftGraphContentType|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get contentTypes from sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + contentTypeId - The unique identifier of contentType
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved navigation property 
    remote isolated function getContentType(string siteId, string listId, string contentTypeId, map<string|string[]> headers = {}, *GetContentTypeQueries queries) returns MicrosoftGraphContentType|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/${getEncodedUri(contentTypeId)}`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete navigation property contentTypes for sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + contentTypeId - The unique identifier of contentType
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteContentType(string siteId, string listId, string contentTypeId, DeleteContentTypeHeaders headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/${getEncodedUri(contentTypeId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update the navigation property contentTypes in sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + contentTypeId - The unique identifier of contentType
    # + headers - Headers to be sent with the request 
    # + payload - New navigation property values 
    # + return - Success 
    remote isolated function updateContentType(string siteId, string listId, string contentTypeId, MicrosoftGraphContentType payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/${getEncodedUri(contentTypeId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get base from sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + contentTypeId - The unique identifier of contentType
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved navigation property 
    remote isolated function getContentTypeBase(string siteId, string listId, string contentTypeId, map<string|string[]> headers = {}, *GetContentTypeBaseQueries queries) returns MicrosoftGraphContentType|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/${getEncodedUri(contentTypeId)}/base`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get baseTypes from sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + contentTypeId - The unique identifier of contentType
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved collection 
    remote isolated function listContentTypeBaseTypes(string siteId, string listId, string contentTypeId, map<string|string[]> headers = {}, *ListContentTypeBaseTypesQueries queries) returns MicrosoftGraphContentTypeCollectionResponse|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/${getEncodedUri(contentTypeId)}/baseTypes`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get baseTypes from sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + contentTypeId - The unique identifier of contentType
    # + contentTypeId1 - The unique identifier of contentType
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved navigation property 
    remote isolated function getContentTypeBaseTypes(string siteId, string listId, string contentTypeId, string contentTypeId1, map<string|string[]> headers = {}, *GetContentTypeBaseTypesQueries queries) returns MicrosoftGraphContentType|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/${getEncodedUri(contentTypeId)}/baseTypes/${getEncodedUri(contentTypeId1)}`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get the number of the resource
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + contentTypeId - The unique identifier of contentType
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The count of the resource 
    remote isolated function getContentTypeBaseTypesCount(string siteId, string listId, string contentTypeId, map<string|string[]> headers = {}, *GetContentTypeBaseTypesCountQueries queries) returns string|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/${getEncodedUri(contentTypeId)}/baseTypes/$count`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get columnLinks from sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + contentTypeId - The unique identifier of contentType
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved collection 
    remote isolated function listContentTypeColumnLinks(string siteId, string listId, string contentTypeId, map<string|string[]> headers = {}, *ListContentTypeColumnLinksQueries queries) returns MicrosoftGraphColumnLinkCollectionResponse|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/${getEncodedUri(contentTypeId)}/columnLinks`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create new navigation property to columnLinks for sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + contentTypeId - The unique identifier of contentType
    # + headers - Headers to be sent with the request 
    # + payload - New navigation property 
    # + return - Created navigation property 
    remote isolated function createContentTypeColumnLink(string siteId, string listId, string contentTypeId, MicrosoftGraphColumnLink payload, map<string|string[]> headers = {}) returns MicrosoftGraphColumnLink|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/${getEncodedUri(contentTypeId)}/columnLinks`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get columnLinks from sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + contentTypeId - The unique identifier of contentType
    # + columnLinkId - The unique identifier of columnLink
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved navigation property 
    remote isolated function getContentTypeColumnLink(string siteId, string listId, string contentTypeId, string columnLinkId, map<string|string[]> headers = {}, *GetContentTypeColumnLinkQueries queries) returns MicrosoftGraphColumnLink|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/${getEncodedUri(contentTypeId)}/columnLinks/${getEncodedUri(columnLinkId)}`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete navigation property columnLinks for sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + contentTypeId - The unique identifier of contentType
    # + columnLinkId - The unique identifier of columnLink
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteContentTypeColumnLink(string siteId, string listId, string contentTypeId, string columnLinkId, DeleteContentTypeColumnLinkHeaders headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/${getEncodedUri(contentTypeId)}/columnLinks/${getEncodedUri(columnLinkId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update the navigation property columnLinks in sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + contentTypeId - The unique identifier of contentType
    # + columnLinkId - The unique identifier of columnLink
    # + headers - Headers to be sent with the request 
    # + payload - New navigation property values 
    # + return - Success 
    remote isolated function updateContentTypeColumnLink(string siteId, string listId, string contentTypeId, string columnLinkId, MicrosoftGraphColumnLink payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/${getEncodedUri(contentTypeId)}/columnLinks/${getEncodedUri(columnLinkId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get the number of the resource
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + contentTypeId - The unique identifier of contentType
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The count of the resource 
    remote isolated function getContentTypeColumnLinksCount(string siteId, string listId, string contentTypeId, map<string|string[]> headers = {}, *GetContentTypeColumnLinksCountQueries queries) returns string|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/${getEncodedUri(contentTypeId)}/columnLinks/$count`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get columnPositions from sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + contentTypeId - The unique identifier of contentType
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved collection 
    remote isolated function listContentTypeColumnPositions(string siteId, string listId, string contentTypeId, map<string|string[]> headers = {}, *ListContentTypeColumnPositionsQueries queries) returns MicrosoftGraphColumnDefinitionCollectionResponse|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/${getEncodedUri(contentTypeId)}/columnPositions`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get columnPositions from sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + contentTypeId - The unique identifier of contentType
    # + columnDefinitionId - The unique identifier of columnDefinition
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved navigation property 
    remote isolated function getContentTypeColumnPosition(string siteId, string listId, string contentTypeId, string columnDefinitionId, map<string|string[]> headers = {}, *GetContentTypeColumnPositionQueries queries) returns MicrosoftGraphColumnDefinition|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/${getEncodedUri(contentTypeId)}/columnPositions/${getEncodedUri(columnDefinitionId)}`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get the number of the resource
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + contentTypeId - The unique identifier of contentType
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The count of the resource 
    remote isolated function getContentTypeColumnPositionsCount(string siteId, string listId, string contentTypeId, map<string|string[]> headers = {}, *GetContentTypeColumnPositionsCountQueries queries) returns string|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/${getEncodedUri(contentTypeId)}/columnPositions/$count`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get columns from sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + contentTypeId - The unique identifier of contentType
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved collection 
    remote isolated function listContentTypeColumns(string siteId, string listId, string contentTypeId, map<string|string[]> headers = {}, *ListContentTypeColumnsQueries queries) returns MicrosoftGraphColumnDefinitionCollectionResponse|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/${getEncodedUri(contentTypeId)}/columns`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create new navigation property to columns for sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + contentTypeId - The unique identifier of contentType
    # + headers - Headers to be sent with the request 
    # + payload - New navigation property 
    # + return - Created navigation property 
    remote isolated function createContentTypeColumn(string siteId, string listId, string contentTypeId, MicrosoftGraphColumnDefinition payload, map<string|string[]> headers = {}) returns MicrosoftGraphColumnDefinition|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/${getEncodedUri(contentTypeId)}/columns`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get columns from sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + contentTypeId - The unique identifier of contentType
    # + columnDefinitionId - The unique identifier of columnDefinition
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved navigation property 
    remote isolated function getContentTypeColumn(string siteId, string listId, string contentTypeId, string columnDefinitionId, map<string|string[]> headers = {}, *GetContentTypeColumnQueries queries) returns MicrosoftGraphColumnDefinition|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/${getEncodedUri(contentTypeId)}/columns/${getEncodedUri(columnDefinitionId)}`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete navigation property columns for sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + contentTypeId - The unique identifier of contentType
    # + columnDefinitionId - The unique identifier of columnDefinition
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteContentTypeColumn(string siteId, string listId, string contentTypeId, string columnDefinitionId, DeleteContentTypeColumnHeaders headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/${getEncodedUri(contentTypeId)}/columns/${getEncodedUri(columnDefinitionId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update the navigation property columns in sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + contentTypeId - The unique identifier of contentType
    # + columnDefinitionId - The unique identifier of columnDefinition
    # + headers - Headers to be sent with the request 
    # + payload - New navigation property values 
    # + return - Success 
    remote isolated function updateContentTypeColumn(string siteId, string listId, string contentTypeId, string columnDefinitionId, MicrosoftGraphColumnDefinition payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/${getEncodedUri(contentTypeId)}/columns/${getEncodedUri(columnDefinitionId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get sourceColumn from sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + contentTypeId - The unique identifier of contentType
    # + columnDefinitionId - The unique identifier of columnDefinition
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved navigation property 
    remote isolated function getContentTypeColumnSourceColumn(string siteId, string listId, string contentTypeId, string columnDefinitionId, map<string|string[]> headers = {}, *GetContentTypeColumnSourceColumnQueries queries) returns MicrosoftGraphColumnDefinition|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/${getEncodedUri(contentTypeId)}/columns/${getEncodedUri(columnDefinitionId)}/sourceColumn`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get the number of the resource
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + contentTypeId - The unique identifier of contentType
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The count of the resource 
    remote isolated function getContentTypeColumnsCount(string siteId, string listId, string contentTypeId, map<string|string[]> headers = {}, *GetContentTypeColumnsCountQueries queries) returns string|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/${getEncodedUri(contentTypeId)}/columns/$count`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Invoke action associateWithHubSites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + contentTypeId - The unique identifier of contentType
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - Success 
    remote isolated function associateContentTypeWithHubSites(string siteId, string listId, string contentTypeId, ContentTypeIdMicrosoftGraphAssociateWithHubSitesBody payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/${getEncodedUri(contentTypeId)}/microsoft.graph.associateWithHubSites`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Invoke action copyToDefaultContentLocation
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + contentTypeId - The unique identifier of contentType
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - Success 
    remote isolated function copyContentTypeToDefaultContentLocation(string siteId, string listId, string contentTypeId, ContentTypeIdMicrosoftGraphCopyToDefaultContentLocationBody payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/${getEncodedUri(contentTypeId)}/microsoft.graph.copyToDefaultContentLocation`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Invoke function isPublished
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + contentTypeId - The unique identifier of contentType
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function isContentTypePublished(string siteId, string listId, string contentTypeId, map<string|string[]> headers = {}) returns BooleanValueResponse|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/${getEncodedUri(contentTypeId)}/microsoft.graph.isPublished()`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Invoke action publish
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + contentTypeId - The unique identifier of contentType
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function publishContentType(string siteId, string listId, string contentTypeId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/${getEncodedUri(contentTypeId)}/microsoft.graph.publish`;
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Invoke action unpublish
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + contentTypeId - The unique identifier of contentType
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function unpublishContentType(string siteId, string listId, string contentTypeId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/${getEncodedUri(contentTypeId)}/microsoft.graph.unpublish`;
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get the number of the resource
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The count of the resource 
    remote isolated function getContentTypesCount(string siteId, string listId, map<string|string[]> headers = {}, *GetContentTypesCountQueries queries) returns string|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/$count`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Invoke action addCopy
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - Success 
    remote isolated function addContentTypeCopy(string siteId, string listId, ContentTypesMicrosoftGraphAddCopyBody payload, map<string|string[]> headers = {}) returns ContentTypeOrNullResponse|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/microsoft.graph.addCopy`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Invoke action addCopyFromContentTypeHub
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - Success 
    remote isolated function addContentTypeCopyFromContentTypeHub(string siteId, string listId, ContentTypesMicrosoftGraphAddCopyFromContentTypeHubBody payload, map<string|string[]> headers = {}) returns ContentTypeOrNullResponse|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/microsoft.graph.addCopyFromContentTypeHub`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Invoke function getCompatibleHubContentTypes
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function getCompatibleHubContentTypes(string siteId, string listId, map<string|string[]> headers = {}, *GetCompatibleHubContentTypesQueries queries) returns CollectionOfContentType|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/contentTypes/microsoft.graph.getCompatibleHubContentTypes()`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$orderby": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get createdByUser from sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved navigation property 
    remote isolated function getListCreatedByUser(string siteId, string listId, map<string|string[]> headers = {}, *GetListCreatedByUserQueries queries) returns MicrosoftGraphUser|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/createdByUser`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get mailboxSettings property value
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Entity result 
    remote isolated function getCreatedByUserMailboxSettings(string siteId, string listId, map<string|string[]> headers = {}, *GetCreatedByUserMailboxSettingsQueries queries) returns MicrosoftGraphMailboxSettings|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/createdByUser/mailboxSettings`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Update property mailboxSettings value.
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + payload - New property values 
    # + return - Success 
    remote isolated function updateCreatedByUserMailboxSettings(string siteId, string listId, MicrosoftGraphMailboxSettings payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/createdByUser/mailboxSettings`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get serviceProvisioningErrors property value
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved collection 
    remote isolated function listCreatedByUserServiceProvisioningErrors(string siteId, string listId, map<string|string[]> headers = {}, *ListCreatedByUserServiceProvisioningErrorsQueries queries) returns MicrosoftGraphServiceProvisioningErrorCollectionResponse|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/createdByUser/serviceProvisioningErrors`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get the number of the resource
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The count of the resource 
    remote isolated function getCreatedByUserServiceProvisioningErrorsCount(string siteId, string listId, map<string|string[]> headers = {}, *GetCreatedByUserServiceProvisioningErrorsCountQueries queries) returns string|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/createdByUser/serviceProvisioningErrors/$count`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get drive from sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved navigation property 
    remote isolated function getListDrive(string siteId, string listId, map<string|string[]> headers = {}, *GetListDriveQueries queries) returns MicrosoftGraphDrive|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/drive`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # List items
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved collection 
    remote isolated function listItems(string siteId, string listId, map<string|string[]> headers = {}, *ListItemsQueries queries) returns MicrosoftGraphListItemCollectionResponse|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a new item in a list
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + payload - New navigation property 
    # + return - Created navigation property 
    remote isolated function createItem(string siteId, string listId, MicrosoftGraphListItem payload, map<string|string[]> headers = {}) returns MicrosoftGraphListItem|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get listItem
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved navigation property 
    remote isolated function getItem(string siteId, string listId, string listItemId, map<string|string[]> headers = {}, *GetItemQueries queries) returns MicrosoftGraphListItem|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete an item from a list
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteItem(string siteId, string listId, string listItemId, DeleteItemHeaders headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update the navigation property items in sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + headers - Headers to be sent with the request 
    # + payload - New navigation property values 
    # + return - Success 
    remote isolated function updateItem(string siteId, string listId, string listItemId, MicrosoftGraphListItem payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get analytics from sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved navigation property 
    remote isolated function getItemAnalytics(string siteId, string listId, string listItemId, map<string|string[]> headers = {}, *GetItemAnalyticsQueries queries) returns MicrosoftGraphItemAnalytics|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/analytics`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get createdByUser from sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved navigation property 
    remote isolated function getItemCreatedByUser(string siteId, string listId, string listItemId, map<string|string[]> headers = {}, *GetItemCreatedByUserQueries queries) returns MicrosoftGraphUser|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/createdByUser`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get mailboxSettings property value
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Entity result 
    remote isolated function getItemCreatedByUserMailboxSettings(string siteId, string listId, string listItemId, map<string|string[]> headers = {}, *GetItemCreatedByUserMailboxSettingsQueries queries) returns MicrosoftGraphMailboxSettings|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/createdByUser/mailboxSettings`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Update property mailboxSettings value.
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + headers - Headers to be sent with the request 
    # + payload - New property values 
    # + return - Success 
    remote isolated function updateItemCreatedByUserMailboxSettings(string siteId, string listId, string listItemId, MicrosoftGraphMailboxSettings payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/createdByUser/mailboxSettings`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get serviceProvisioningErrors property value
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved collection 
    remote isolated function listItemCreatedByUserServiceProvisioningErrors(string siteId, string listId, string listItemId, map<string|string[]> headers = {}, *ListItemCreatedByUserServiceProvisioningErrorsQueries queries) returns MicrosoftGraphServiceProvisioningErrorCollectionResponse|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/createdByUser/serviceProvisioningErrors`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get the number of the resource
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The count of the resource 
    remote isolated function getItemCreatedByUserServiceProvisioningErrorsCount(string siteId, string listId, string listItemId, map<string|string[]> headers = {}, *GetItemCreatedByUserServiceProvisioningErrorsCountQueries queries) returns string|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/createdByUser/serviceProvisioningErrors/$count`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # List documentSetVersions
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved collection 
    remote isolated function listItemDocumentSetVersions(string siteId, string listId, string listItemId, map<string|string[]> headers = {}, *ListItemDocumentSetVersionsQueries queries) returns MicrosoftGraphDocumentSetVersionCollectionResponse|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/documentSetVersions`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create documentSetVersion
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + headers - Headers to be sent with the request 
    # + payload - New navigation property 
    # + return - Created navigation property 
    remote isolated function createItemDocumentSetVersion(string siteId, string listId, string listItemId, MicrosoftGraphDocumentSetVersion payload, map<string|string[]> headers = {}) returns MicrosoftGraphDocumentSetVersion|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/documentSetVersions`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get documentSetVersion
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + documentSetVersionId - The unique identifier of documentSetVersion
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved navigation property 
    remote isolated function getItemDocumentSetVersion(string siteId, string listId, string listItemId, string documentSetVersionId, map<string|string[]> headers = {}, *GetItemDocumentSetVersionQueries queries) returns MicrosoftGraphDocumentSetVersion|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/documentSetVersions/${getEncodedUri(documentSetVersionId)}`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete documentSetVersion
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + documentSetVersionId - The unique identifier of documentSetVersion
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteItemDocumentSetVersion(string siteId, string listId, string listItemId, string documentSetVersionId, DeleteItemDocumentSetVersionHeaders headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/documentSetVersions/${getEncodedUri(documentSetVersionId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update the navigation property documentSetVersions in sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + documentSetVersionId - The unique identifier of documentSetVersion
    # + headers - Headers to be sent with the request 
    # + payload - New navigation property values 
    # + return - Success 
    remote isolated function updateItemDocumentSetVersion(string siteId, string listId, string listItemId, string documentSetVersionId, MicrosoftGraphDocumentSetVersion payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/documentSetVersions/${getEncodedUri(documentSetVersionId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get fields from sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + documentSetVersionId - The unique identifier of documentSetVersion
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved navigation property 
    remote isolated function getItemDocumentSetVersionFields(string siteId, string listId, string listItemId, string documentSetVersionId, map<string|string[]> headers = {}, *GetItemDocumentSetVersionFieldsQueries queries) returns MicrosoftGraphFieldValueSet|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/documentSetVersions/${getEncodedUri(documentSetVersionId)}/fields`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete navigation property fields for sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + documentSetVersionId - The unique identifier of documentSetVersion
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteItemDocumentSetVersionFields(string siteId, string listId, string listItemId, string documentSetVersionId, DeleteItemDocumentSetVersionFieldsHeaders headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/documentSetVersions/${getEncodedUri(documentSetVersionId)}/fields`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update the navigation property fields in sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + documentSetVersionId - The unique identifier of documentSetVersion
    # + headers - Headers to be sent with the request 
    # + payload - New navigation property values 
    # + return - Success 
    remote isolated function updateItemDocumentSetVersionFields(string siteId, string listId, string listItemId, string documentSetVersionId, MicrosoftGraphFieldValueSet payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/documentSetVersions/${getEncodedUri(documentSetVersionId)}/fields`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Invoke action restore
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + documentSetVersionId - The unique identifier of documentSetVersion
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function restoreItemDocumentSetVersion(string siteId, string listId, string listItemId, string documentSetVersionId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/documentSetVersions/${getEncodedUri(documentSetVersionId)}/microsoft.graph.restore`;
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get the number of the resource
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The count of the resource 
    remote isolated function getItemDocumentSetVersionsCount(string siteId, string listId, string listItemId, map<string|string[]> headers = {}, *GetItemDocumentSetVersionsCountQueries queries) returns string|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/documentSetVersions/$count`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get driveItem from sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved navigation property 
    remote isolated function getItemDriveItem(string siteId, string listId, string listItemId, map<string|string[]> headers = {}, *GetItemDriveItemQueries queries) returns MicrosoftGraphDriveItem|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/driveItem`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get content for the navigation property driveItem from sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved media content 
    remote isolated function getItemDriveItemContent(string siteId, string listId, string listItemId, map<string|string[]> headers = {}, *GetItemDriveItemContentQueries queries) returns byte[]|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/driveItem/content`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Update content for the navigation property driveItem in sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + headers - Headers to be sent with the request 
    # + payload - New media content 
    # + return - Success 
    remote isolated function updateItemDriveItemContent(string siteId, string listId, string listItemId, byte[] payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/driveItem/content`;
        http:Request request = new;
        request.setPayload(payload, "application/octet-stream");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Get fields from sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved navigation property 
    remote isolated function getItemFields(string siteId, string listId, string listItemId, map<string|string[]> headers = {}, *GetItemFieldsQueries queries) returns MicrosoftGraphFieldValueSet|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/fields`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete navigation property fields for sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteItemFields(string siteId, string listId, string listItemId, DeleteItemFieldsHeaders headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/fields`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update listItem
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + headers - Headers to be sent with the request 
    # + payload - New navigation property values 
    # + return - Success 
    remote isolated function updateItemFields(string siteId, string listId, string listItemId, MicrosoftGraphFieldValueSet payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/fields`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get lastModifiedByUser from sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved navigation property 
    remote isolated function getItemLastModifiedByUser(string siteId, string listId, string listItemId, map<string|string[]> headers = {}, *GetItemLastModifiedByUserQueries queries) returns MicrosoftGraphUser|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/lastModifiedByUser`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get mailboxSettings property value
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Entity result 
    remote isolated function getItemLastModifiedByUserMailboxSettings(string siteId, string listId, string listItemId, map<string|string[]> headers = {}, *GetItemLastModifiedByUserMailboxSettingsQueries queries) returns MicrosoftGraphMailboxSettings|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/lastModifiedByUser/mailboxSettings`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Update property mailboxSettings value.
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + headers - Headers to be sent with the request 
    # + payload - New property values 
    # + return - Success 
    remote isolated function updateItemLastModifiedByUserMailboxSettings(string siteId, string listId, string listItemId, MicrosoftGraphMailboxSettings payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/lastModifiedByUser/mailboxSettings`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get serviceProvisioningErrors property value
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved collection 
    remote isolated function listItemLastModifiedByUserServiceProvisioningErrors(string siteId, string listId, string listItemId, map<string|string[]> headers = {}, *ListItemLastModifiedByUserServiceProvisioningErrorsQueries queries) returns MicrosoftGraphServiceProvisioningErrorCollectionResponse|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/lastModifiedByUser/serviceProvisioningErrors`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get the number of the resource
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The count of the resource 
    remote isolated function getItemLastModifiedByUserServiceProvisioningErrorsCount(string siteId, string listId, string listItemId, map<string|string[]> headers = {}, *GetItemLastModifiedByUserServiceProvisioningErrorsCountQueries queries) returns string|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/lastModifiedByUser/serviceProvisioningErrors/$count`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Invoke action createLink
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - Success 
    remote isolated function createItemLink(string siteId, string listId, string listItemId, ListItemIdMicrosoftGraphCreateLinkBody payload, map<string|string[]> headers = {}) returns PermissionOrNullResponse|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/microsoft.graph.createLink`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Invoke function getActivitiesByInterval
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function getItemActivitiesByInterval(string siteId, string listId, string listItemId, map<string|string[]> headers = {}, *GetItemActivitiesByIntervalQueries queries) returns CollectionOfItemActivityStat|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/microsoft.graph.getActivitiesByInterval()`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$orderby": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Invoke function getActivitiesByInterval
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + startDateTime - Usage: startDateTime='{startDateTime}'
    # + endDateTime - Usage: endDateTime='{endDateTime}'
    # + interval - Usage: interval='{interval}'
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function getItemActivitiesByIntervalWithDates(string siteId, string listId, string listItemId, string? startDateTime, string? endDateTime, string? interval, map<string|string[]> headers = {}, *GetItemActivitiesByIntervalWithDatesQueries queries) returns CollectionOfItemActivityStat|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/microsoft.graph.getActivitiesByInterval(startDateTime='${getEncodedUri(startDateTime)}',endDateTime='${getEncodedUri(endDateTime)}',interval='${getEncodedUri(interval)}')`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$orderby": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Listing versions of a ListItem
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved collection 
    remote isolated function listItemVersions(string siteId, string listId, string listItemId, map<string|string[]> headers = {}, *ListItemVersionsQueries queries) returns MicrosoftGraphListItemVersionCollectionResponse|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/versions`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create new navigation property to versions for sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + headers - Headers to be sent with the request 
    # + payload - New navigation property 
    # + return - Created navigation property 
    remote isolated function createItemVersion(string siteId, string listId, string listItemId, MicrosoftGraphListItemVersion payload, map<string|string[]> headers = {}) returns MicrosoftGraphListItemVersion|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/versions`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a ListItemVersion resource
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + listItemVersionId - The unique identifier of listItemVersion
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved navigation property 
    remote isolated function getItemVersion(string siteId, string listId, string listItemId, string listItemVersionId, map<string|string[]> headers = {}, *GetItemVersionQueries queries) returns MicrosoftGraphListItemVersion|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/versions/${getEncodedUri(listItemVersionId)}`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete navigation property versions for sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + listItemVersionId - The unique identifier of listItemVersion
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteItemVersion(string siteId, string listId, string listItemId, string listItemVersionId, DeleteItemVersionHeaders headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/versions/${getEncodedUri(listItemVersionId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update the navigation property versions in sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + listItemVersionId - The unique identifier of listItemVersion
    # + headers - Headers to be sent with the request 
    # + payload - New navigation property values 
    # + return - Success 
    remote isolated function updateItemVersion(string siteId, string listId, string listItemId, string listItemVersionId, MicrosoftGraphListItemVersion payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/versions/${getEncodedUri(listItemVersionId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get fields from sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + listItemVersionId - The unique identifier of listItemVersion
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved navigation property 
    remote isolated function getItemVersionFields(string siteId, string listId, string listItemId, string listItemVersionId, map<string|string[]> headers = {}, *GetItemVersionFieldsQueries queries) returns MicrosoftGraphFieldValueSet|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/versions/${getEncodedUri(listItemVersionId)}/fields`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete navigation property fields for sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + listItemVersionId - The unique identifier of listItemVersion
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteItemVersionFields(string siteId, string listId, string listItemId, string listItemVersionId, DeleteItemVersionFieldsHeaders headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/versions/${getEncodedUri(listItemVersionId)}/fields`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update the navigation property fields in sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + listItemVersionId - The unique identifier of listItemVersion
    # + headers - Headers to be sent with the request 
    # + payload - New navigation property values 
    # + return - Success 
    remote isolated function updateItemVersionFields(string siteId, string listId, string listItemId, string listItemVersionId, MicrosoftGraphFieldValueSet payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/versions/${getEncodedUri(listItemVersionId)}/fields`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Invoke action restoreVersion
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + listItemVersionId - The unique identifier of listItemVersion
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function restoreItemVersion(string siteId, string listId, string listItemId, string listItemVersionId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/versions/${getEncodedUri(listItemVersionId)}/microsoft.graph.restoreVersion`;
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get the number of the resource
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + listItemId - The unique identifier of listItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The count of the resource 
    remote isolated function getItemVersionsCount(string siteId, string listId, string listItemId, map<string|string[]> headers = {}, *GetItemVersionsCountQueries queries) returns string|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/${getEncodedUri(listItemId)}/versions/$count`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Invoke function delta
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function getItemsDelta(string siteId, string listId, map<string|string[]> headers = {}, *GetItemsDeltaQueries queries) returns CollectionOfListItem|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/microsoft.graph.delta()`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$orderby": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Invoke function delta
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + token - Usage: token='{token}'
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function getItemsDeltaWithToken(string siteId, string listId, string? token, map<string|string[]> headers = {}, *GetItemsDeltaWithTokenQueries queries) returns CollectionOfListItem|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/items/microsoft.graph.delta(token='${getEncodedUri(token)}')`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$orderby": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get lastModifiedByUser from sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved navigation property 
    remote isolated function getListLastModifiedByUser(string siteId, string listId, map<string|string[]> headers = {}, *GetListLastModifiedByUserQueries queries) returns MicrosoftGraphUser|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/lastModifiedByUser`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get mailboxSettings property value
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Entity result 
    remote isolated function getLastModifiedByUserMailboxSettings(string siteId, string listId, map<string|string[]> headers = {}, *GetLastModifiedByUserMailboxSettingsQueries queries) returns MicrosoftGraphMailboxSettings|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/lastModifiedByUser/mailboxSettings`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Update property mailboxSettings value.
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + payload - New property values 
    # + return - Success 
    remote isolated function updateLastModifiedByUserMailboxSettings(string siteId, string listId, MicrosoftGraphMailboxSettings payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/lastModifiedByUser/mailboxSettings`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get serviceProvisioningErrors property value
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved collection 
    remote isolated function listLastModifiedByUserServiceProvisioningErrors(string siteId, string listId, map<string|string[]> headers = {}, *ListLastModifiedByUserServiceProvisioningErrorsQueries queries) returns MicrosoftGraphServiceProvisioningErrorCollectionResponse|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/lastModifiedByUser/serviceProvisioningErrors`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get the number of the resource
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The count of the resource 
    remote isolated function getLastModifiedByUserServiceProvisioningErrorsCount(string siteId, string listId, map<string|string[]> headers = {}, *GetLastModifiedByUserServiceProvisioningErrorsCountQueries queries) returns string|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/lastModifiedByUser/serviceProvisioningErrors/$count`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get operations from sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved collection 
    remote isolated function listOperations(string siteId, string listId, map<string|string[]> headers = {}, *ListOperationsQueries queries) returns MicrosoftGraphRichLongRunningOperationCollectionResponse|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/operations`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create new navigation property to operations for sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + payload - New navigation property 
    # + return - Created navigation property 
    remote isolated function createOperation(string siteId, string listId, MicrosoftGraphRichLongRunningOperation payload, map<string|string[]> headers = {}) returns MicrosoftGraphRichLongRunningOperation|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/operations`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get operations from sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + richLongRunningOperationId - The unique identifier of richLongRunningOperation
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved navigation property 
    remote isolated function getOperation(string siteId, string listId, string richLongRunningOperationId, map<string|string[]> headers = {}, *GetOperationQueries queries) returns MicrosoftGraphRichLongRunningOperation|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/operations/${getEncodedUri(richLongRunningOperationId)}`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete navigation property operations for sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + richLongRunningOperationId - The unique identifier of richLongRunningOperation
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteOperation(string siteId, string listId, string richLongRunningOperationId, DeleteOperationHeaders headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/operations/${getEncodedUri(richLongRunningOperationId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update the navigation property operations in sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + richLongRunningOperationId - The unique identifier of richLongRunningOperation
    # + headers - Headers to be sent with the request 
    # + payload - New navigation property values 
    # + return - Success 
    remote isolated function updateOperation(string siteId, string listId, string richLongRunningOperationId, MicrosoftGraphRichLongRunningOperation payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/operations/${getEncodedUri(richLongRunningOperationId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get the number of the resource
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The count of the resource 
    remote isolated function getOperationsCount(string siteId, string listId, map<string|string[]> headers = {}, *GetOperationsCountQueries queries) returns string|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/operations/$count`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get subscriptions from sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved collection 
    remote isolated function listSubscriptions(string siteId, string listId, map<string|string[]> headers = {}, *ListSubscriptionsQueries queries) returns MicrosoftGraphSubscriptionCollectionResponse|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/subscriptions`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create new navigation property to subscriptions for sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + payload - New navigation property 
    # + return - Created navigation property 
    remote isolated function createSubscription(string siteId, string listId, MicrosoftGraphSubscription payload, map<string|string[]> headers = {}) returns MicrosoftGraphSubscription|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/subscriptions`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get subscriptions from sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + subscriptionId - The unique identifier of subscription
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved navigation property 
    remote isolated function getSubscription(string siteId, string listId, string subscriptionId, map<string|string[]> headers = {}, *GetSubscriptionQueries queries) returns MicrosoftGraphSubscription|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/subscriptions/${getEncodedUri(subscriptionId)}`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete navigation property subscriptions for sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + subscriptionId - The unique identifier of subscription
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteSubscription(string siteId, string listId, string subscriptionId, DeleteSubscriptionHeaders headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/subscriptions/${getEncodedUri(subscriptionId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update the navigation property subscriptions in sites
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + subscriptionId - The unique identifier of subscription
    # + headers - Headers to be sent with the request 
    # + payload - New navigation property values 
    # + return - Success 
    remote isolated function updateSubscription(string siteId, string listId, string subscriptionId, MicrosoftGraphSubscription payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/subscriptions/${getEncodedUri(subscriptionId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Invoke action reauthorize
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + subscriptionId - The unique identifier of subscription
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function reauthorizeSubscription(string siteId, string listId, string subscriptionId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/subscriptions/${getEncodedUri(subscriptionId)}/microsoft.graph.reauthorize`;
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get the number of the resource
    #
    # + siteId - The unique identifier of site
    # + listId - The unique identifier of list
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The count of the resource 
    remote isolated function getSubscriptionsCount(string siteId, string listId, map<string|string[]> headers = {}, *GetSubscriptionsCountQueries queries) returns string|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/${getEncodedUri(listId)}/subscriptions/$count`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get the number of the resource
    #
    # + siteId - The unique identifier of site
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The count of the resource 
    remote isolated function getListsCount(string siteId, map<string|string[]> headers = {}, *GetListsCountQueries queries) returns string|error {
        string resourcePath = string `/${getEncodedUri(siteId)}/lists/$count`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }
}
